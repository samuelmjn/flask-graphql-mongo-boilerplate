language: python

python:
  - 3.6
  - 3.5
  - 3.4
  - 2.7

before_install:
  - pip install cookiecutter
  - cookiecutter . --no-input

install: pip install -u tox-travis

script: tox -c 'flask-graphql-mongo'/tox.ini
